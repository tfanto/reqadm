select processname,
    crtdat,
	crtusr,
	chgdat,
	chgusr,
	dltdat,
	dltusr,
	chgnbr,
	crtver,
	description,
	shortdescr, 
	tenant,
	version,
	productname,
	topicname,
	processname,	
	processseq
from process 
where 
	tenant = ? and
	version=? and 
	productname=? and 
	topicname=? and 
	processname=? and 
	processseq=?
	
	 
	
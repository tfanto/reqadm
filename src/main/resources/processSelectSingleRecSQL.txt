select processname,
		processseq, 
		description,
		crtdat,
		chgnbr,
		shortdescr,
		crtusr,
		chgdat,
		chgusr,
		crtver,
		dltdat,
		dltusr
from process 
where 
	productname=? and 
	topicname=? and 
	processname=? and 
	processseq=? and 
	version=? and 
	tenant = ?
	
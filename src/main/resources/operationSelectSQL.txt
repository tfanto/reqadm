select 
	crtdat,
	crtusr,
	chgdat,
	chgusr,
	dltdat,
	dltusr,
	chgnbr,
	crtver,
	description,
	shortdescr,
	tenant,
	version,
	productname,
	topicname,
	processname,
	processseq, 
	operationname,
	operationseq 
from oper 
   where
	  tenant=? and 
	  version=? and
	  productname=?  and 
      topicname=? and 
   	  processname=? and 
	  processseq=?   
order by 
	productname,
	topicname,
	processname,
	processseq,
	operationname,
	operationseq
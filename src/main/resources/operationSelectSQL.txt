select 
	topicname,
	processname,
	processseq, 
	operationname,
	operationseq, 
	description,
	crtdat,
	chgnbr,
	shortdescr,
	crtusr,
	chgdat,
	chgusr,
	crtver,
	dltdat,
	dltusr 
from oper 
	where productname=?  and 
	topicname=? and 
	processname=? and 
	processseq=?  and 
	version=? and
	tenant=? 
order by 
	productname,
	topicname,
	processname,
	processseq,
	operationname,
	operationseq